
// need to do this instead of just "sake" because I haven't installed it locally yet
var sake = require("../../../../");

// Standard module exports
module.exports = {
    foo: function (t) {
        sake.log("Hello World: " + t.name);
    }
};

