
require("sake/clean");

require("sake/vm").run(function () {
    CLOBBER = new FileList();
    desc("Remove all files.");
    task("clobber", ["clean"], function (t) {
        CLOBBER.exisiting.forEach(function (path) {
            rm_rf(path);
        });
    });
});